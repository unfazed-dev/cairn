# wasm-pack output — generated from source by `wasm-pack build`. The smoke
# test rebuilds it; committing it would drift on every code change.
pkg/
# `--target nodejs` output consumed by sdk/cairn_web (same rationale).
pkg-node/
